const targetPath = './target/';
const outputPath = './dist/';
const outputFile = 'manifestmd5.txt';

module.exports = {
	TARGET_PATH: targetPath,
	OUTPUT_PATH: outputPath,
	OUTPUT_FILE: outputFile
};
